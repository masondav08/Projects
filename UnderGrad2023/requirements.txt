#this is a requiremnts file for the cyber tools suite.
#Remember to use "pip install -r requirements.txt" once cmd is in the directory \\final-project-masondav08
#please ensure IDE does not have import issues, use windows recommended interpreter if necessary.

cryptography==3.4.7
requests==2.26.0
beautifulsoup4==4.10.0
prettytable==2.1.0
colorama==0.4.4
